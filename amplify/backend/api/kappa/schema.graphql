type MyType @model {
	id: ID!
	title: String!
	content: String!
}

type SlotResult {
	first: String
	second: String
	third: String
	winner: Boolean
}

type Mutation {
	runSlotmachine(user: String!): SlotResult @function(name: "slotfunction-${env}")
}